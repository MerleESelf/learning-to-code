// YOUR CODE BELOW
// funct that will take  up to 4 strings for arg.
// needs to take the first character for each string to make a new string. This
// must account for undefined args too and

const myMnemonic = (str1, str2, str3, str4) => {
    let newWord = ''; 
    // want statements that will account for undefined strings
    if( str1 !== undefined && str2 !== undefined && str3 !== undefined && str4 !== undefined) { 
        return newWord += str1[0] + str2[0] + str3[0] + str4[0]; 
    } else if (s)
    
}
